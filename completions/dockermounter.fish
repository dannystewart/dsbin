# Auto-generated completions for dockermounter
complete -c dockermounter -l check -d "only check share status without making changes" -f
complete -c dockermounter -l docker -d "path to docker-compose.yml, to restart services if desired."
complete -c dockermounter -l auto -d "don't prompt for confirmation before cleaning shares" -f
