# Auto-generated completions for bounces
complete -c bounces -s d -l dir -d "directory to search"
complete -c bounces -l suffix -d "filter by suffix"
complete -c bounces -l format -d "filter by file format"
complete -c bounces -l title -d "filter by title (substring match)" -f
complete -c bounces -l per-day -d "show latest bounce for each day" -f
complete -c bounces -l include-suffixed -d "include suffixed bounces when determining latest" -f
