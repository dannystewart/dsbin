# Auto-generated completions for ssh-tunnel
complete -c ssh-tunnel -d "the host for the SSH tunnel." -f
complete -c ssh-tunnel -d "the port number for the SSH tunnel" -f
complete -c ssh-tunnel -s l -l list -d "list all active SSH tunnels" -f
complete -c ssh-tunnel -l kill -d "kill the SSH tunnel on the specified port" -f
complete -c ssh-tunnel -l kill-all -d "kill all active SSH tunnels" -f
complete -c ssh-tunnel -l local-port -d "local port number for the SSH tunnel (optional)" -f
complete -c ssh-tunnel -s u -l user -d "username for SSH tunnel" -f
