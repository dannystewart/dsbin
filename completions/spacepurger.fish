# Auto-generated completions for spacepurger
complete -c spacepurger -s t -l fill-until -d "Fill disk until this much free space remains in GB (default: 1)" -f
complete -c spacepurger -s d -l max-duration -d "Maximum duration in minutes (default: 30)" -f
complete -c spacepurger -s m -l monitor-only -d "Only monitor space recovery, don't create files" -f
complete -c spacepurger -l no-cleanup -d "Don't automatically cleanup temp files (for testing)" -f
