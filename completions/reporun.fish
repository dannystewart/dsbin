# Auto-generated completions for reporun
complete -c reporun -a "update lock hooks status custom" -d "command to execute across packages"
complete -c reporun -d "custom command to run (required with 'custom')"
complete -c reporun -l packages -d "specific packages to process (defaults to all)"
complete -c reporun -l dir -d "base directory containing package folders"
