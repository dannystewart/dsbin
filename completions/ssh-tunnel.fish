# Auto-generated completions for ssh-tunnel
complete -c ssh-tunnel -d "the host for the SSH tunnel."
complete -c ssh-tunnel -d "the port number for the SSH tunnel"
complete -c ssh-tunnel -s l -l list -d "list all active SSH tunnels"
complete -c ssh-tunnel -l kill -d "kill the SSH tunnel on the specified port"
complete -c ssh-tunnel -l kill-all -d "kill all active SSH tunnels"
complete -c ssh-tunnel -l local-port -d "local port number for the SSH tunnel (optional)"
complete -c ssh-tunnel -s u -l user -d "username for SSH tunnel (default: current user)"
